# If your server uses CUDA, install the matching PyTorch build first:
#   https://pytorch.org/get-started/locally/
#
# Then install:
#   python -m pip install -r requirements.txt

torch>=2.4,<3.0
numpy>=1.26,<3.0
scipy>=1.11,<2.0
tqdm>=4.66,<5.0
