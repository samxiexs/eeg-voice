# Install the CUDA-matched PyTorch build before running:
#   python -m pip install -r requirements.txt
#
# The version ranges below are intentionally broad because the server image may
# already include a newer CUDA-enabled PyTorch 2.x build.

torch>=2.4,<3.0
torchaudio>=2.4,<3.0

numpy>=1.26
pandas>=2.1
scipy>=1.11
mne>=1.7

requests>=2.31
openpyxl>=3.1
matplotlib>=3.8
pytest>=8.0
tqdm>=4.66
