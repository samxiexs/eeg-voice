# v2 aligned data route: both DS004940 tasks and a relaxed bad-channel limit.
# Five participants declare 20-23 bad electrodes (of 128); with interpolation
# and the per-channel validity mask they are usable, so the limit is raised
# from 0.15 (19 channels) to 0.20 (25 channels).
extends: training_data_v4_ds004940_fixed.yaml
schema_version: training-data-aligned-v2
output_root: artifacts/training_data/aligned_v2
split_seed: eeg-recon-0809-aligned-v2

harmonized:
  interpolation:
    max_bad_fraction: 0.20
